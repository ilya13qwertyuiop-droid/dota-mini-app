{
  "query": "query HeroPairs($weekStart: DateTime!, $weekEnd: DateTime!) { /* paste the existing browser collector query here */ }",
  "variables": {
    "weekStart": "{{week_start}}",
    "weekEnd": "{{week_end}}"
  },
  "records_path": "data.heroPairs",
  "hero_id_field": "heroId",
  "pair_id_field": "otherHeroId",
  "vs_field": "vs",
  "with_field": "with",
  "synergy_field": "synergy",
  "match_count_field": "matchCount"
}
